[project]
name = "limner"
version = "0.1.0"
description = "A framework for portable agent personas, with tools for VGA-era pixel art generation"
requires-python = ">=3.10"

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501", "E402"]
