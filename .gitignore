# Node / TypeScript
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
dist/
build/
*.tsbuildinfo
.tsbuildinfo

# Wrangler / Cloudflare Workers
.wrangler/
.dev.vars
.dev.vars.*
# ...except the committed template the Deploy button reads secret names from.
!.dev.vars.example
worker-configuration.d.ts

# Environment
.env
.env.local
.env.*.local

# Migration outputs — the *.ts script is the committed artifact; the
# generated SQL (and any local CMA memory export) is regenerable and
# may contain memory content, so it stays out of git.
migrations/generated/
cma-memory-export.json

# Maintainer-local files — never committed: scrub-check pattern list
# (would reintroduce the strings it guards against) and the production
# wrangler overlay with real resource IDs and routes.
.scrub-patterns
packages/limner-mcp/wrangler.prod.toml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs / temp
*.log
tmp/
.tmp/

# Test coverage
coverage/
.nyc_output/

# Test 4 asset-generation harness: inputs (real source photos) and outputs
# (generated assets + transcripts) — never committed; assets route to Drive.
test-runs/
