# Node / TypeScript
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
dist/
build/
*.tsbuildinfo
.tsbuildinfo

# Wrangler / Cloudflare Workers
.wrangler/
.dev.vars
.dev.vars.*
worker-configuration.d.ts

# Environment
.env
.env.local
.env.*.local

# Migration outputs — the *.ts script is the committed artifact; the
# generated SQL (and any local CMA memory export) is regenerable and
# may contain memory content, so it stays out of git.
migrations/generated/
cma-memory-export.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs / temp
*.log
tmp/
.tmp/

# Test coverage
coverage/
.nyc_output/
